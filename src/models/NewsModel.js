export default class NewsModel {
  constructor(title, abstract, url, byline, thumbnail) {
    this.title = title;
    this.abstract = abstract;
    this.url = url;
    this.byline = byline;
    this.thumbnail = thumbnail;
  }
}